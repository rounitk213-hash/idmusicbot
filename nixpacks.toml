[phases.setup]
nixPkgs = ["ffmpeg", "python311", "gcc", "gnumake", "libffi", "openssl"]

[phases.install]
cmds = ["python -m pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "python3 main.py"
