[phases.setup]
nixPkgs = ["ffmpeg", "python310", "gcc", "gnumake", "libffi", "openssl", "nodejs"]

[phases.install]
cmds = ["python -m pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "python3 main.py"
